# TODO

- [x] Search and identify all places where a logo image is referenced (img tags, CSS background-image, etc.).
- [x] Update those places to use the existing asset `src/assets/logo.JPG` (imported as a module in React components).
- [x] Ensure the image renders correctly on the Welcome page and any other existing layout components (Sidebar).
- [x] Run the project (build/lint) to confirm no broken imports or missing assets.


